---
description: "[Alias → /director] Compile script.md into storyboard.json."
argument-hint: <project_id>
---

Project: $1

This command is now an alias. Follow `/director $1` instead.

Read the `video-director` skill and execute the director command workflow:
compile `projects/$1/script.md` into `storyboard.json` with scene planning,
shot compilation, validation, and estimation.
